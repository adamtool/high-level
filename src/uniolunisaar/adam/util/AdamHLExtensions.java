package uniolunisaar.adam.util;

/**
 * This enum contains all by the high-level submodule used keys for saving
 * object via the Extensible interface of APT
 *
 * @author Manuel Gieseking
 */
public enum AdamHLExtensions implements IAdamExtensions {
    colorDomain,
    colorTokens,
    predicate,
    arcExpression,
    convOrigID,
    convColors,
    convValuation,
    convTransitionMapping
}
